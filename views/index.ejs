<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
</head>
<body>
    <a href="new">New Message</a>

    <ul>
        <% for (let i = 0; i < messages.length; i++) { %>
            <li>
                <%= messages[i].user %> (<%= messages[i].added %>): <%= messages[i].text %>
            </li>
       <% } %>
    </ul>
    
    
</body>
</html>